export enum JobName {
  INGEST_DOCUMENT = 'ingest:document',
  INGEST_URL = 'ingest:url',
  INGEST_PUBLIC = 'ingest:public',
  GENERATE_EMBEDDINGS = 'embed:generate',
  WEBHOOK_DISPATCH = 'webhook:dispatch',
}
